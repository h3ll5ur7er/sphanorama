

/**
 * The commit this bundle was built from, replaced by `define` at build time. Declared rather than
 * imported because it does not exist as a module: `vite dev` leaves the identifier undeclared, so
 * every read of it has to survive not being there.
 */
declare const __SPHANORAMA_BUILD__: string | undefined;
