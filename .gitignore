# build
build/
dist/
node_modules/
.emscripten_cache/
*.wasm
*.o
*.a

# tooling
__pycache__/
.venv/
.cache/
.DS_Store

# datasets and captures (large, regenerate with tools/)
datasets/
captures/

# playwright
test-results/
playwright-report/

# the emsdk action stages its cache beside the sources, in both workflows
emsdk-cache/

# staged build artifacts
shell/public/core/
dist/

# Agent worktrees. A worktree here is a whole second checkout of this repository, node_modules
# and all; it is scratch space for a background agent, never something to commit.
.claude/worktrees/
